pluginManagement {
    repositories {
        maven {
            name = 'Fabric'
            url = 'https://maven.fabricmc.net/'
        }

        gradlePluginPortal()
    }
}

rootProject.name = 'corduroy'

// corduroy-example excluded from the 26.1 build (demo mod, not ported)